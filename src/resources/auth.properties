usersByQuery=select login, password, true from users where login = ?
rolesByQuery=select u.login, ur.role_name from users u join users_roles_ties ur_ties on u.user_id = ur_ties.user_id join roles ur on ur_ties.role_id = ur.role_id WHERE u.login = ?