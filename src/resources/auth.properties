usersByQuery=select login, password, true from users where login = ?
rolesByQuery=select u.login, ur.role_name from users u,roles ur,users_roles_ties ur_ties WHERE u.user_id = ur_ties.user_id and ur.role_id = ur_ties.role_id and u.login = ?